let add a b = a + b
let message = "orange"

let result = add 2 3
let _ = print_endline (message ^ " -> " ^ string_of_int result)

# balanced repo language weight
# keep each language section equally visible in GitHub

# balanced repo language weight
# keep each language section equally visible in GitHub
